function repeatStringNumTimes(str, num) {
    let storage = ""
    for(let i = 0; i < num; i++){
      
      // return str += str;
     storage += str
    }
      return storage
  }
  repeatStringNumTimes("*", 8);
